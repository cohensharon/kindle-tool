# article-to-kindle

CLI tool for sending extracted article EPUBs to a Kindle email address.

## Kindle Email Setup

Create a local `.env` file using `.env.example` as a template.

The `FROM_EMAIL` sender address must be approved in your Amazon Kindle settings before Kindle will accept emailed EPUB attachments.
